extends layout

block content
  h1 #{title}: #{book.title}

  if book_instance.length

    p #[strong Delete the following bookinstanced before attempting to delete this author.]

    div(style='margin-left:20px;margin-top:20px')

      h4 Bookinstances

      dl
      each bookinstance in book_instances
        dt
          
